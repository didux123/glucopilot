// Copier en Config/Local.xcconfig et renseigner votre Team ID Apple.
// Le trouver : Xcode > Settings > Accounts > votre compte > Team ID,
// ou `security find-certificate -c "Apple Development: ..." -p | openssl x509 -noout -subject`
// (le champ OU=).
DEVELOPMENT_TEAM = XXXXXXXXXX
